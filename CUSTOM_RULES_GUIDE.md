# 添加自定义规则指南

Metis 支持多种方式添加自定义安全审查规则。以下是三种主要方法：

## 方法 1: 使用 `.metis.md` 文件（推荐，最简单）

在项目根目录（`codebase-path` 指定的目录）创建 `.metis.md` 文件，Metis 会自动加载并使用其中的内容作为自定义指导。

### 示例 `.metis.md` 文件：

```markdown
# 自定义安全审查规则

## 重点关注的安全问题

1. **敏感信息泄露**
   - 检查硬编码的 API 密钥、密码、令牌
   - 检查日志中是否包含敏感信息
   - 检查错误消息是否泄露系统信息

2. **输入验证**
   - 所有用户输入必须经过验证和清理
   - 特别注意文件路径、URL、命令参数
   - 检查 SQL 注入、XSS、命令注入风险

3. **认证和授权**
   - 验证所有需要权限的操作都有适当的检查
   - 检查会话管理是否安全
   - 验证权限提升的可能性

4. **加密和哈希**
   - 检查是否使用弱加密算法
   - 验证密码是否使用安全的哈希函数（如 bcrypt, argon2）
   - 检查随机数生成器是否安全

5. **依赖安全**
   - 检查是否使用已知存在漏洞的第三方库
   - 验证依赖版本是否及时更新

## 特定规则

- 禁止使用 `eval()` 或类似的动态代码执行函数
- 所有数据库查询必须使用参数化查询
- 文件操作必须验证路径，防止目录遍历攻击
- 所有网络请求必须验证 SSL/TLS 证书
```

### 使用方法：

```bash
# Metis 会自动查找并使用 .metis.md 文件
uv run metis --codebase-path ./target
```

## 方法 2: 使用 `--custom-prompt` 参数

创建一个 Markdown 或文本文件，然后通过命令行参数指定：

```bash
uv run metis --codebase-path ./target --custom-prompt ./my-custom-rules.md
```

### 示例 `my-custom-rules.md` 文件：

```markdown
# 我的自定义规则

重点关注以下安全问题：
- 内存安全问题（缓冲区溢出、use-after-free）
- 并发安全问题（竞态条件、死锁）
- 资源泄漏（内存泄漏、文件句柄泄漏）
```

## 方法 3: 修改 `plugins.yaml`（高级用法）

如果你想为特定编程语言添加永久性的规则，可以修改 `src/metis/plugins/plugins.yaml` 文件。

### 步骤：

1. 找到对应语言的配置（例如 `c:` 部分）
2. 修改 `security_review_checks` 字段

### 示例：为 C 语言添加自定义规则

编辑 `src/metis/plugins/plugins.yaml`，找到 `c:` 部分：

```yaml
c:
  supported_extensions: [".c", ".h", ".cc"]
  splitting: &c_cpp_splitting
    chunk_lines: 40
    chunk_lines_overlap: 15
    max_chars: 1500
  prompts: &c_cpp_prompts
    security_review_checks: |-
      2. What to Check
         - Look for potential security issues such as:
           - Memory safety issues
           - Buffer overflows
           # 在这里添加你的自定义规则
           - 自定义规则 1: 检查所有 malloc 调用是否有对应的 free
           - 自定义规则 2: 验证所有数组访问是否在边界内
           - 自定义规则 3: 检查所有指针解引用前是否进行了空指针检查
```

## 规则优先级

根据 `plugins.yaml` 中的 `custom_guidance_precedence` 设置：

- **自定义指导优先**：如果自定义规则与默认规则冲突，自定义规则优先
- **如果自定义指导排除了某些问题类别**：这些类别将被忽略，除非需要解释其他问题

## 最佳实践

1. **使用 `.metis.md` 文件**：这是最简单且不需要修改源码的方法
2. **规则要具体**：提供清晰的示例和说明
3. **规则要可操作**：确保 LLM 能够理解和应用这些规则
4. **定期更新**：根据项目需求和安全最佳实践更新规则

## 示例：完整的 `.metis.md` 文件

```markdown
# 项目特定安全审查规则

## 核心安全原则

1. **最小权限原则**：所有功能应遵循最小权限原则
2. **深度防御**：多层安全控制
3. **安全默认值**：默认配置应该是安全的

## 必须检查的问题

### 1. 输入验证
- [ ] 所有外部输入都经过验证
- [ ] 使用白名单而非黑名单
- [ ] 验证数据类型、长度、格式

### 2. 输出编码
- [ ] 所有输出都经过适当的编码
- [ ] 防止 XSS 攻击
- [ ] 防止注入攻击

### 3. 错误处理
- [ ] 错误消息不泄露敏感信息
- [ ] 错误处理不会导致信息泄露
- [ ] 异常被正确捕获和处理

### 4. 日志记录
- [ ] 不记录敏感信息（密码、令牌、个人信息）
- [ ] 日志级别适当
- [ ] 日志文件权限正确

## 特定于本项目的规则

- 所有 API 调用必须包含超时设置
- 所有数据库操作必须使用事务
- 所有文件上传必须验证文件类型和大小
- 所有密码必须使用 bcrypt 进行哈希
```

## 验证规则是否生效

运行 Metis 后，检查输出中的问题描述，应该能看到你的自定义规则被应用。例如，如果你添加了"检查所有 malloc 调用是否有对应的 free"规则，Metis 应该会报告相关的内存泄漏问题。

